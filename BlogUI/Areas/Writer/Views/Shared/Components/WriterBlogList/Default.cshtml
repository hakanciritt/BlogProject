@model List<Blog>

<div class="row">
    <div class="col-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Son 5 Blog</h4>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th> Görsel </th>
                                <th> Başlık </th>
                                <th> Durum </th>
                                <th> Oluşturulma Tarihi </th>
                                <th> Blog Detay </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var blog in Model.OrderByDescending(x=>x.BlogId).Take(5))
                            {
                                <tr>
                                    <td>
                                        <img src="~/writerpanel/assets/images/faces/face1.jpg" class="mr-2" alt="image">
                                    </td>
                                    <td> @blog.Title </td>
                                    <td>
                                        <label class="badge badge-gradient-success">True</label>
                                    </td>
                                    <td> @blog.CreateDate.ToString("dd-MMM-yyyy") </td>
                                    <td> <a target="_blank" class="btn btn-primary" asp-controller="Blog" asp-action="BlogDetails" asp-route-blogSlug="@blog.Slug" >Detay</a> </td>
                                </tr>
                            }

                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
